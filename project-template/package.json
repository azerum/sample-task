{
    "name": "@azerum/PROJECT_NAME",
    "version": "1.0.0",
    
    "scripts": {
        "build": "tsc -p ./tsconfig.build.json",
        "full-build": "rush build --to @azerum/PROJECT_NAME",
        "deploy": "rush deploy -p @azerum/PROJECT_NAME -t ./deploy --overwrite",
        "ship": "rushx full-build && rushx deploy"
    },

    "devDependencies": {
        "@azerum/base-tsconfig": "workspace:*"
    }
}
